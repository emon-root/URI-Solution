#include<stdio.h>

int main()
{
    int T, i, student, mark;
    scanf("%d", T);
    for(i=1; i<=T; i++){
        scanf("%d", &student);
        for(i=1; i<=student;i++){
            scanf("%d", &mark);

        }
    }
}
