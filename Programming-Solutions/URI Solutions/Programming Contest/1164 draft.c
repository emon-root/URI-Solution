#include<stdio.h>

int main()
{
    int T, i, n,a, c, d;
    scanf("%d", &T);
    for(i=1; i<=T; i++){
        scanf("%d", &n);
        c = n/2; d=0;
        for(a=1; a<=c; a++){
            if(n%a==0)
                d+=b;
        }
        if()
    }
}
